{
  "name": "hotels-mono",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "main": "index.js",
  "scripts": {
    "build": "yarn workspaces run build",
    "dev": "yarn workspaces run dev",
    "types": "yarn workspace @hotels-mono/backend types"
  },
  "dependencies": {},
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.6",
    "@types/node": "^20.12.7",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
